# Secure DNS

> A simple DNS server written in Go using DNS over HTTPS along with some graceful fallback options

## Features
- [x] DNS over HTTPS
- [ ] Fallback over UDP
- [ ] Fallback over TCP
- [ ] Systemd Daemon
